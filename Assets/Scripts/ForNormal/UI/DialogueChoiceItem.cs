using UnityEngine;
using UnityEngine.UI;

/// <summary>
/// 选项条目，可按需扩展（例如选中态背景、图标等）。
/// 该脚本不是必需，仅作为标记与扩展点。
/// </summary>
public class DialogueChoiceItem : MonoBehaviour
{
    public Image background;
    public Text uiText;
}
